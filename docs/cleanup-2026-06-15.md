# 仓库清理清单（开源前）— 2026-06-15

本文件列出开源前建议清理/整理的文件，**待用户确认后执行删除**。

## A. 明确误提交 / 垃圾（建议删除）

| 路径 | 大小 | 判断依据 |
|---|---|---|
| `:memory:/history.sqlite`（含整个 `:memory:/` 目录） | 60K | `:memory:` 是 SQLite 内存库占位符，被误当成真实路径，DB 文件误提交 |
| `CollectionMonitorPage.tsx` | 68K | 来自其他项目的孤立文件，`import '../../api/client'` / `./taskActions` 在本仓库不存在，全仓库零引用 |
| `verify_usage.js` | 2K | 根目录临时 Playwright 调试脚本，零引用 |
| `.logs/local-agent.pid`、`.logs/web.pid` | - | 运行时 PID 文件误提交，应改为 gitignore |
| `.codex-nexra-mvp.png` | 48K | 根目录隐藏临时截图 |

## B. 开源敏感 / 需决策

| 路径 | 说明 |
|---|---|
| `scripts/experiments/copilot-private-probe/` | 跟踪部分约 668K。逆向探测 GitHub Copilot 私有存储 / token 的实验，与主项目无关，开源存在 ToS / 法律观感风险。建议移除或迁至独立私有仓库 |
| `AGENTS.md` / `CLAUDE.md` | 内部 "Nexra" agent 工作流规则。开源可保留、精简或删除 |
| `ui-design.png`（1.7M） / `icons/prev/` | 设计稿大图 / 图标预览图，确认是否仍需 |

## C. 整理规范（非删除）

- `docs/changelog/`（8 个） 与 `docs/changelogs/`（7 个）命名重复，建议合并为一个目录
- 缺 `LICENSE` 文件 — 开源必备，需补充
- `.gitignore` 需补充 `*.pid`、`:memory:/`
- `.env` 已正确 gitignore（含密钥，未被提交）✅

## 执行结果（用户确认后）

- **A 类全部删除**：`:memory:/`、`CollectionMonitorPage.tsx`、`verify_usage.js`、`.logs/*.pid`、`.codex-nexra-mvp.png`
- **copilot-private-probe 整目录删除**（含 49M 被忽略的 `.vscode-probe-user-data` 缓存，未提交）
- **被取代的 Copilot/VS Code 逆向设计文档删除**：`vscode-state-mirror` 与 `ai-session-stream` 的 spec+plan（4 份）
- **AGENTS.md 删除，保留 CLAUDE.md**
- **`docs/changelogs/` 合并进 `docs/changelog/`**（共 15 份）
- **新增 `LICENSE`（MIT）**
- **`.gitignore` 补充** `*.pid`、`.logs`、`:memory:/`
- **README 更新**：补 mobile 客户端、修正过时的「配对码」为密码登录、新增安全提示、License 指向 LICENSE 文件
- **保留**：`ui-design.png`、`icons/prev/`、`dev-control` spec/plan、`apps/mobile`

## 第二轮：转纯 Web，移除移动端壳（用户确认）

- **删除 `apps/shell`**（Capacitor Android APK 壳，5 文件）+ `scripts/build-apk.sh`
- **删除 `apps/mobile`**（Flutter 原生端，111 文件 + 磁盘构建产物）
- **删除 `docs/acceptance.md`**（原始 v0.1 验收，误标 VS Code/Flutter 已交付）
- **Makefile**：移除 `apk` 目标
- **package.json**：name → `claude-console`、更新 description、移除 `test:mobile`/`test:all`
- **文档改写为纯 Web**：README、install、testing、architecture、web、mobile、remote-access、publish
- **security.md**：鉴权章节改写为当前模型（密码登录 + token + MAC_AGENT_BIND），去掉配对码/VS Code/PTY 过时描述
- **tokens.ts**：注释去掉 Flutter/apps/mobile 引用
- **保留**：`docs/vscode-extension.md`（已是正确的"已移除"墓碑页）、`scripts/e2e-smoke.sh`（`/auth/pair` 端点服务端仍在）
- 验证：`pnpm install` 移除 261 个壳依赖；`pnpm test` 共 107（shared 15 + local-agent 74 + web 18）

## 第三轮：移除公网反代，Tailscale 直连为唯一路径（用户确认）

- **remote-access.md 重写**：删掉 nginx/socat/Caddy 反代与公网服务器，改为 Tailscale 直连
  （Mac 绑 `MAC_AGENT_BIND=$(tailscale ip -4)`，手机同 tailnet 浏览器直连 `:3005` / `:7345`）
- **README / architecture.md**：拓扑图改为「浏览器 ──Tailscale WireGuard──▶ Mac」，去掉公网服务器节点
- **security.md**：威胁模型/加密项改为 Tailscale 端到端加密，不暴露公网
- **publish.md / Makefile / web.md**：去掉 reverse-proxy 提示、修正过时「配对码」为密码登录
- 净效果：开源用户 2 步即可远程接入，无需租服务器、无需 TLS

## 验证记录

- 全仓库 `grep` 确认 `CollectionMonitorPage`、`verify_usage` 无引用
- `git ls-files` 确认上述文件的跟踪状态
- 跟踪文件体积排序：`ui-design.png`(1.7M) > `copilot-storage-report.json`(440K) > `pnpm-lock.yaml`(232K)
