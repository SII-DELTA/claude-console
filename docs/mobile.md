# 移动端（纯 Web / PWA）

不做原生端。客户端 = [Web 控制台](web.md)（apps/web，Claude Code 风格、电脑/手机双适配、PWA）。

手机端直接用浏览器打开服务器上的 web，iOS Safari / Android Chrome 均可「添加到主屏幕」当 App 用，
无需安装包、无需签名。早期的 Flutter 原生端与 Capacitor APK 壳已移除。
